package com.example.l03_3;

import org.junit.Test;

import java.util.Arrays;
import java.util.Collections;
import java.util.HashSet;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }

    public void test() {
       /* MathService math = new MathService();
        Integer a[] = { 1,2,3 };
        Integer b[] = { 3,4,5};
        Integer c[] = {1,2,3,4,5};
        HashSet set = new HashSet();
        Collections.addAll(set = new HashSet(Arrays.asList(a)));

        // initializing set using Collections.unmodifiable set
        HashSet set2 = new HashSet();
        Collections.addAll(set2 = new HashSet(Arrays.asList(b)));

        HashSet res = new HashSet();
        Collections.addAll(res = new HashSet(Arrays.asList(b)));
        assert (res, math.setUnion(set, set2)); */
    }

}